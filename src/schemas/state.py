# src/schemas/state.py
# Pydantic model for graph state validation (mirrors graph/state.py TypedDict).
# Used to validate state payloads at node boundaries during testing.
#
# TODO: implement RuleBankStateModel Pydantic model
